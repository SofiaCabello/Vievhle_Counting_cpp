#pragma once
#include "geometry.h"
#include "data_processing.h"
#include <vector>
#include <string>

using namespace std;

void visualize(string, vector<polygon>&, vector <vector<rect>>&, vector<trail>&, int);